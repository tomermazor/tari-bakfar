# 🌿 טרי בכפר — מדריך התקנה מלא

## סקירה כללית

המערכת מורכבת מ-3 חלקים:
1. **Google Sheet** — מסד הנתונים שלך (חינמי, של Google)
2. **Google Apps Script** — הקוד שמחבר בין האפליקציה לגיליון (חינמי)
3. **קובץ HTML** — האפליקציה עצמה (רץ על הטלפון/מחשב)

זמן ההתקנה: **30-45 דקות**, פעם אחת.

---

## שלב 1: יצירת Google Sheet (5 דקות)

1. היכנס ל-[sheets.google.com](https://sheets.google.com)
2. לחץ על **+** ליצירת גיליון ריק חדש
3. בחר את שם הגיליון בפינה השמאלית-עליונה ושנה ל: **טרי בכפר - מעקב הוצאות**

✅ אל תוסיף עמודות או טבלאות — הקוד יעשה את זה אוטומטית.

---

## שלב 2: הוספת Apps Script (10 דקות)

1. בתוך הגיליון שיצרת, לחץ על תפריט: **Extensions** ← **Apps Script**
2. תיפתח חלון חדש של עורך הקוד
3. **מחק את כל הקוד הקיים** (אמור להופיע משהו כמו `function myFunction() { }`)
4. **פתח את הקובץ `google-apps-script.gs`** שקיבלת ממני
5. **העתק את כל התוכן והדבק** בעורך הקוד
6. שמור: **Ctrl+S** או **Cmd+S**
7. תן לפרויקט שם: **טרי בכפר** ← Save

---

## שלב 3: הרצת ההתקנה הראשונית (3 דקות)

1. עדיין בעורך ה-Apps Script
2. בחלק העליון, ליד הכפתור "Run", יש dropdown של פונקציות
3. בחר את הפונקציה: **`setup`**
4. לחץ על כפתור **Run** (▶️)
5. **Google יבקש הרשאות:**
   - לחץ "Review permissions"
   - בחר את חשבון Google שלך
   - אם תופיע אזהרת "Google hasn't verified this app", לחץ על "Advanced" ← "Go to טרי בכפר (unsafe)" — **זה בסדר, זה הקוד שלך**
   - אשר את ההרשאות (Google Drive, Sheets)
6. כשהפונקציה תרוץ בהצלחה, תראה: ✅ "Execution completed"
7. **חזור לטאב של הגיליון** — תראה שנוצרו 3 גיליונות חדשים: הוצאות, הוצאות קבועות, תשלומים

---

## שלב 4: פרסום כ-Web App (5 דקות)

זה השלב שיוצר את ה-URL שהאפליקציה תתחבר אליו.

1. בעורך ה-Apps Script, לחץ על **Deploy** (פינה ימנית למעלה)
2. בחר **New deployment**
3. ליד "Select type" לחץ על גלגל השיניים ⚙️ ← בחר **Web app**
4. מלא את הטופס:
   - **Description:** טרי בכפר API
   - **Execute as:** Me (your-email@gmail.com)
   - **Who has access:** **Anyone** ⚠️ (חובה — אחרת האפליקציה לא תוכל לגשת)
5. לחץ **Deploy**
6. Google יבקש שוב הרשאות — אשר
7. **תקבל URL כזה:**
   ```
   https://script.google.com/macros/s/AKfycb.../exec
   ```
8. **העתק את ה-URL** ושמור אותו — זה נדרש בשלב הבא

> ⚠️ **חשוב:** אם בעתיד תרצה לשנות משהו בקוד, תצטרך לעשות "New deployment" שוב, או "Manage deployments" ← Edit ← Version: New version

---

## שלב 5: התקנת האפליקציה בטלפון (5 דקות)

### דרך 1: פתיחה מהטלפון

1. שלח לעצמך את הקובץ `tari-bakfar.html` ב-WhatsApp/Email/AirDrop
2. פתח אותו בטלפון — הוא ייפתח בדפדפן (Safari/Chrome)
3. **ב-iPhone:** Safari ← כפתור שיתוף (⤴️) ← "**הוספה למסך הבית**" ← תן שם "טרי בכפר" ← Add
4. **ב-Android:** Chrome ← תפריט (⋮) ← "**הוסף למסך הבית**"
5. עכשיו יש לך אייקון אפליקציה במסך הבית 🎉

### דרך 2: העלאה ל-Drive (אופציה אלטרנטיבית)

1. העלה את `tari-bakfar.html` ל-Google Drive שלך
2. פתח אותו דרך Drive בטלפון
3. הוסף למסך הבית כמו למעלה

---

## שלב 6: חיבור האפליקציה (2 דקות)

1. פתח את האפליקציה בטלפון
2. לחץ על ⚙️ **הגדרות** בתפריט התחתון
3. בכרטיס "☁️ חיבור ל-Google Sheets":
   - הדבק את ה-URL מ-שלב 4
   - לחץ **🔍 בדוק** — אמור להופיע "✅ החיבור עובד!"
   - לחץ **💾 שמור**
4. בכרטיס "🔑 OpenAI API Key":
   - הדבק את ה-API Key מ-OpenAI
   - לחץ **💾 שמור מפתח**

🎉 **סיימת! עכשיו האפליקציה מסונכרנת עם Google Sheets שלך.**

---

## איך זה עובד

### זרימת נתונים

```
מצלם חשבונית בטלפון
        ↓
ה-AI (OpenAI) מחלץ פרטים
        ↓
האפליקציה שולחת ל-Google Apps Script
        ↓
התמונה עולה ל-Google Drive שלך
        ↓
הנתונים נכנסים אוטומטית ל-Google Sheets
```

### גיבוי

- **כל הנתונים נשמרים גם מקומית** בטלפון (לעבודה offline)
- **וגם בענן** ב-Google Sheets שלך
- אם הטלפון נשבר — תפתח את האפליקציה במכשיר חדש, תחבר שוב את ה-URL, **והנתונים יסתנכרנו אוטומטית**
- Google שומר היסטוריית שינויים — אפשר לשחזר נתונים גם אם נמחקו בטעות

### עבודה offline

- אם אין רשת — האפליקציה תמשיך לעבוד עם הנתונים המקומיים
- שינויים שתעשה ייכנסו לתור (תראה "⚠ אופליין" בכותרת)
- ברגע שיהיה אינטרנט שוב — הכל יסתנכרן אוטומטית

---

## פתרון בעיות

### "החיבור נכשל" בבדיקה
- ודא שב-Deployment בחרת **"Anyone"** לגישה
- ודא שהעתקת את ה-URL במלואו, כולל `/exec` בסוף
- נסה לפתוח את ה-URL ישירות בדפדפן — אמור להופיע "טרי בכפר API פעיל"

### הנתונים לא מסתנכרנים
- לחץ "🔄 סנכרן עכשיו" בהגדרות
- בדוק שיש אינטרנט
- בדוק שה-Sheet עדיין קיים ולא נמחק בטעות

### תמונות לא נשמרות
- ייתכן ש-Google חוסם בגלל גודל — ודא שאישרת את כל ההרשאות ב-Deployment
- התמונות נשמרות בתיקייה "טרי בכפר - חשבוניות" ב-Drive שלך

### "שגיאת רשת" בסריקת AI
- ודא שיש לך יתרה בחשבון OpenAI (platform.openai.com → Billing)
- ודא שה-API Key תקין ומופעל

---

## עלויות

| שירות | עלות |
|------|------|
| Google Sheets, Drive, Apps Script | **חינם** |
| OpenAI API | ~3-5 ₪/חודש לפי שימוש |
| **סה"כ** | **~3-5 ₪/חודש** |

---

## טיפים

- **שתף עם רואה החשבון:** פתח את ה-Sheet ב-Google Sheets ← Share ← הוסף את האימייל שלו עם הרשאת Viewer
- **דוחות ב-Sheets:** ב-Sheets עצמו אתה יכול ליצור Pivot Tables וגרפים מתקדמים
- **ייצוא לאקסל:** File ← Download ← Microsoft Excel
- **חיפוש מהיר:** השתמש בחיפוש המובנה של Sheets (Ctrl+F)

---

## עזרה נוספת

אם נתקלת בבעיה — פתח את ה-Apps Script editor, לחץ על **View** ← **Executions** כדי לראות לוגים של מה שקרה בבקשה האחרונה.
